{% extends 'base.html.twig' %}

{% block title %}UsedProduct{% endblock %}

{% block body %}
    <h1>UsedProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ used_product.id }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ used_product.weight }}</td>
            </tr>
            <tr>
                <th>Capacity</th>
                <td>{{ used_product.capacity }}</td>
            </tr>
            <tr>
                <th>Expiration</th>
                <td>{{ used_product.expiration ? used_product.expiration|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('used_product_index') }}">back to list</a>

    <a href="{{ path('used_product_edit', {'id': used_product.id}) }}">edit</a>

    {{ include('used_product/_delete_form.html.twig') }}
{% endblock %}

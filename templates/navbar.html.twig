{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('home')}}">Home</a>
    <a href="{{ path('app_logout')}}">Wyloguj</a>
    <a href="{{ path('user_show', {'id': app.user.id}) }}">Profil Użytkownika</a>

{% endblock %}
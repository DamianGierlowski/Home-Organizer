<form method="post" action="{{ path('used_product_delete', {'id': used_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ used_product.id) }}">
    <button class="btn">Delete</button>
</form>
